$kind: http-request
name: 'Import Transactions'
order: 4000
method: POST
url: '{{base_url}}/import'
body:
  type: formdata
  content:
    - key: file
      type: file
      src: 'transactions.csv'
      description: 'CSV file containing transactions to import'
